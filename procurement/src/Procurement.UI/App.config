<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <configSections>
    <section name="entityFramework" type="System.Data.Entity.Internal.ConfigFile.EntityFrameworkSection, EntityFramework, Version=6.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089" requirePermission="false" />
  </configSections>

  <startup>
    <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.8" />
  </startup>

  <!--
    No static connection string here anymore: LoginForm resolves it at runtime (Windows
    identity + MAX company selection -> ProcurementConnectionStringHelper builds the
    "Unitron"/"Unitron_test" connection string), then CompositionRoot passes it straight into
    `new ProcurementDbContext(connectionString)`. The <providers> registration below is still
    needed regardless — it's how EF6 knows which SqlProviderServices to use for a
    System.Data.SqlClient connection string, whatever that string's actual value is.
  -->
  <entityFramework>
    <providers>
      <provider invariantName="System.Data.SqlClient" type="System.Data.Entity.SqlServer.SqlProviderServices, EntityFramework.SqlServer" />
    </providers>
  </entityFramework>

  <!--
    TODO (spec §1 / §0 punt 2): zodra echte DigiKey/Farnell API-credentials beschikbaar zijn,
    voeg ze hier toe (of beter: in een secrets manager, niet in App.config) en zet
    UseMockData op false in CompositionRoot. Nooit plaintext credentials in de audit-log
    wegschrijven — DbAuditLogger maskeert bekende secret-sleutelnamen, maar voeg nieuwe
    velden hier met dezelfde voorzichtigheid toe.
  -->
  <appSettings>
    <add key="DigiKey.ClientId" value="TODO-not-set" />
    <add key="DigiKey.ClientSecret" value="TODO-not-set" />
    <add key="DigiKey.IsSandbox" value="true" />
    <add key="Farnell.ApiKey" value="TODO-not-set" />
    <add key="Farnell.IsSandbox" value="true" />
  </appSettings>

  <runtime>
    <assemblyBinding xmlns="urn:schemas-microsoft-com:asm.v1">
      <dependentAssembly>
        <assemblyIdentity name="EntityFramework" publicKeyToken="b77a5c561934e089" culture="neutral" />
        <!-- EntityFramework.dll's AssemblyVersion has stayed pinned at 6.0.0.0 across every 6.x NuGet release (6.1, 6.2, 6.3, 6.4.x) -->
        <bindingRedirect oldVersion="0.0.0.0-6.0.0.0" newVersion="6.0.0.0" />
      </dependentAssembly>
    </assemblyBinding>
  </runtime>
</configuration>
